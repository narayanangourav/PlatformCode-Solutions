def count_substring(string, sub_string):
    count=0
    n=len(sub_string)
    for i in range(len(string)):
        val=string[i:i+n]
        if i+3 == -1:
            break
        if val == sub_string: 
            count += 1
        continue
    return count
