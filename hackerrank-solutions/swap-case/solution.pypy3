def swap_case(s):
    temp=s
    temp=list(temp)
    for i in range(0,len(s)):
        if temp[i].isupper():
            temp[i]=str(temp[i].lower())
        elif temp[i].islower():
            temp[i]=temp[i].upper()
    new = ""
    for x in temp:
        new += x
    return new
